import styled from "styled-components";

export default function Header() {
  return <SytledHeader>Marks Codeing Challenge</SytledHeader>;
}

const SytledHeader = styled.header`
  font-size: 4rem;
  color: pink;
`;
